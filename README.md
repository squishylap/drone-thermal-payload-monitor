# drone-thermal-payload-monitor
Python-based thermal image analysis system that detects overheating in drone payload sections using NumPy and OpenCV — extracts thermal gradients, isolates hotspots, and triggers real-time overheat alerts with logged coordinates and timestamps.
